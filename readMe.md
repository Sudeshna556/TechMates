# TechMates
- create react + vite application
- Install atinwind and set it up

- added navbar component
- setup react-roter-dom
    - npm i react-router-dom
    - import BrowserRouter then inside it wrap the app, provide a base name which is relative to all the paths,inside the component use Routes and Route to define the routes.
    - 
    